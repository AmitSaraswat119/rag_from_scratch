# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17

# Environment variables
python-dotenv==1.0.1

# PDF extraction
pdfplumber==0.11.4

# OpenAI (embeddings + generation)
openai==1.55.3

# Zilliz / Milvus client
pymilvus==2.4.9
# pymilvus depends on environs which requires marshmallow 3.x (not 4.x)
marshmallow<4.0.0

# Cross-encoder reranker
sentence-transformers==3.3.1

# Data validation (FastAPI uses this internally)
pydantic==2.10.1
