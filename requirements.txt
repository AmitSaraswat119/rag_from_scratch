# ── Python 3.11 only ─────────────────────────────────────────────────────────
# All versions below are tested and confirmed working on Python 3.11

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17

# Environment variables
python-dotenv==1.0.1

# PDF extraction
pdfplumber==0.11.4

# OpenAI (embeddings + generation)
openai==1.55.3

# Zilliz / Milvus client
pymilvus==2.6.9
# pymilvus requires marshmallow 3.x (not 4.x)
marshmallow==3.26.2

# Cross-encoder reranker
sentence-transformers==3.3.1

# Data validation
pydantic==2.10.1
pydantic-core==2.27.1

# setuptools needed for pkg_resources used by some dependencies
setuptools==80.10.2
